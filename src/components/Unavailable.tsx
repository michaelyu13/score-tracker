import { Typography } from '@mui/material';

const Unavailable = () => {
    return (
        <Typography variant="h6" component="p" mb={2}>
            Unavailable
        </Typography>
    );
};

export default Unavailable;
